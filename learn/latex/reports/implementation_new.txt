\chapter{IMPLEMENTATION DETAILS}

\section{System Overview}
CF-MADRL is a clustered federated multi-agent reinforcement learning system for adaptive traffic signal control. The implementation integrates three connected layers: (1) a SUMO-based simulation environment for training and evaluation, (2) a federated training pipeline that clusters and aggregates agent policies, and (3) a Raspberry Pi deployment stack that serves real-time phase decisions to ESP32 traffic controllers through a lightweight HTTP API. Each controlled junction is treated as an autonomous agent with its own policy, while coordination is achieved through clustered weight aggregation across junctions.

The workflow is organized into three stages. In the training stage, each junction learns locally within a shared SUMO simulation and periodically uploads its weights to the federated server. In the evaluation stage, the learned policies are compared against a fixed-time baseline under the same simulation settings. In the deployment stage, the trained policy is executed on the Raspberry Pi, which receives traffic metrics from ESP32 camera feeds, runs inference, and returns the next phase and duration to the ESP32 controller. This end-to-end loop allows the same control logic to move from simulation into real hardware with minimal changes.

\section{Traffic Simulation Environment}
\subsection{Simulation Topology}
\begin{figure}[h]
  \centering
  \includegraphics[width=0.9\textwidth]{img/simulation_network.png}
  \caption{SUMO network used for training and evaluation.}
  \label{fig:sumo_network}
\end{figure}

The SUMO network contains four controlled intersections (J1, J2, J5, J7). Each junction has its own traffic light program and incoming lanes, and the simulation runs as a single shared environment so that inter-junction traffic interactions can be captured in the shared state.

\begin{table}[H]
  \centering
  \small
  \caption{Junction topology (SUMO).}
  \label{tab:junction_topology}
  \begin{tabular}{|c|c|c|c|}
    \hline
    \textbf{Junction} & \textbf{Incoming Lanes} & \textbf{Total Phases} & \textbf{Green Phases} \\
    \hline
    J1 & 6 lanes & 4 phases & 2 phases \\
    J2 & 6 lanes & 4 phases & 2 phases \\
    J5 & 4 lanes & 4 phases & 2 phases \\
    J7 & 4 lanes & 4 phases & 2 phases \\
    \hline
  \end{tabular}
\end{table}

\subsection{Simulation Parameters}
Simulation settings are defined in \texttt{config.yaml} and shared across training and evaluation. The step length defines the physics update, a gridlock timer ends episodes if vehicles stop clearing, and yellow time is enforced between phase changes. These parameters ensure stability during training and keep evaluation conditions consistent.

\begin{table}[H]
  \centering
  \caption{SUMO simulation parameters.}
  \label{tab:sumo_config}
  \begin{tabular}{|l|c|l|}
    \hline
    \textbf{Parameter} & \textbf{Value} & \textbf{Description} \\
    \hline
    SUMO Physics Step & 1.0 s & Vehicle physics step \\
    Gridlock Detection & 200 s & Episode ends if no vehicles depart for 200s \\
    Yellow Interval & 3 s & Safe green-to-yellow transition \\
    Local Decisions per Round & 1000 & Policy decisions per agent per round \\
    Federated Rounds & 300 & Total federated aggregation cycles \\
    \hline
  \end{tabular}
\end{table}

\section{State, Action, and Reward Design}
\subsection{Observation Space}
For each lane at junction $i$, the observation includes local traffic and shared upstream signals. The state contains halted vehicle counts and cumulative waiting times per lane, plus shared queue and waiting estimates derived from upstream junctions. Shared terms are computed from the latest upstream metrics and weighted by turn ratios estimated from outgoing-lane vehicle counts. The current traffic light phase is appended as a normalized scalar so that the agent is aware of the present control state.

All observations are padded to a fixed dimension using the maximum lane count $L_{\max}=6$:
\begin{equation}
  \mathbf{s}_i^t = [\mathbf{q}_i, \mathbf{w}_i, \mathbf{q}_i^{\text{shared}}, \mathbf{w}_i^{\text{shared}}, \phi_i^{\text{norm}}] \in \mathbb{R}^{25},
\end{equation}
where $\phi_i^{\text{norm}} = \phi_i / \text{max\_phases}$.

\textbf{Normalization.} Constant scaling is used for both training and deployment: $\hat{q}=q/15$ and $\hat{w}=w/300$. The constants 15 and 300 are computed from the number of vehicles observed in the network. Shared components are weighted by $\alpha_s=0.5$ before scaling and all values are clipped to $[-10,10]$.

\subsection{Action Space}
Each agent selects a discrete action encoding both green phase and green duration. The action space size is $4 \times 7 = 28$ options (max phases $\times$ durations). Effective actions depend on the junctions available green phases (2 per junction). The durations used in training are $\{30,40,50,60,80,100,120\}$ seconds, and actions are decoded as:
\begin{align}
  \text{duration\_idx} &= a \bmod 7 \\
  \text{green\_phase\_idx} &= \lfloor a/7 \rfloor \bmod \text{num\_green\_phases} \\
  \text{duration} &= [30,40,50,60,80,100,120][\text{duration\_idx}] \\
  \text{phase} &= \text{green\_phases}[\text{green\_phase\_idx}]
\end{align}

\subsection{Reward Function}
Rewards are computed per junction using queue length and waiting time, with waiting time weighted to stabilize training:
\begin{equation}
  r_i = -\frac{\text{total queue} + 0.5 \times \text{total waiting time}}{\text{number of lanes}}.
\end{equation}
A reward floor of $-200.0$ is applied to prevent extreme spikes. This reward structure encourages reduction of congestion while avoiding unstable gradients caused by large waiting-time values.

\section{Agent Design}
\subsection{CF-MADRL (PPO) Agent}
Each junction runs an independent PPO policy (no parameter sharing). Policies share the same observation and action dimensions and are mapped directly by junction ID. The policy network is a two-layer MLP with ReLU activation, and PPO uses a scheduled learning rate and entropy coefficient to balance exploration and convergence.

\begin{table}[H]
  \centering
  \caption{PPO agent configuration (CF-MADRL).}
  \label{tab:ppo_agent_cfg}
  \begin{tabular}{|l|r|l|}
    \hline
    \textbf{Parameter} & \textbf{Value} & \textbf{Description} \\
    \hline
    Observation Dim & 25 & Size of the input state per junction \\
    Action Dim & 28 & Number of possible phase-duration actions \\
    Hidden Layers & [384, 320] (ReLU) & Sizes of the neural network layers \\
    Learning Rate Schedule & 0.0005, 0.00015, 0.00005 & Step size used during training \\
    Entropy Schedule & 0.05, 0.01, 0.001 & Encourages exploration, then reduces it \\
    Train Batch Size & 256 & Samples used for one policy update \\
    Minibatch Size & 64 & Chunk size for each update step \\
    SGD Epochs & 3 & Number of passes per update \\
    Discount Factor $\gamma$ & 0.95 & Importance of future rewards \\
    GAE Lambda $\lambda$ & 0.95 & Smooths advantage estimates \\
    Clip Param $\epsilon$ & 0.2 & Limits overly large policy updates \\
    \hline
  \end{tabular}
\end{table}

\subsection{Q-Table Agent}
The Q-Table baseline discretizes the continuous observation into bins for queues, waits, shared queues, shared waits, and a phase bucket. It uses epsilon-greedy exploration with learning rate and epsilon decay after each round.

\begin{table}[H]
  \centering
  \caption{Q-Table configuration.}
  \label{tab:qtable_cfg}
  \begin{tabular}{|l|r|l|}
    \hline
    \textbf{Parameter} & \textbf{Value} & \textbf{Description} \\
    \hline
    Learning Rate $\alpha$ & 0.0005 & Update size for Q-values \\
    Discount Factor $\gamma$ & 0.95 & Importance of future rewards \\
    Epsilon (start) & 1.0 & Initial randomness in action choice \\
    Epsilon Min & 0.0001 & Minimum exploration level \\
    Epsilon Decay & 0.955 & How fast randomness decreases \\
    Learning Rate Decay & 0.99 & Gradual reduction of update size \\
    Bins & 4 & Number of discretization levels \\
    Bin Edges & [-10.0, -0.5, 0.5, 2.0, 10.0] & Cutoffs for converting values to bins \\
    \hline
  \end{tabular}
\end{table}

\subsection{DQN Agent}
Each junction runs an independent DQN with replay buffer and target network updates. The Q-network uses the same hidden sizes as PPO and is optimized with Adam and MSE loss with gradient clipping (max\_norm=10).

\begin{table}[H]
  \centering
  \caption{DQN configuration.}
  \label{tab:dqn_cfg}
  \begin{tabular}{|l|r|l|}
    \hline
    \textbf{Parameter} & \textbf{Value} & \textbf{Description} \\
    \hline
    Learning Rate & 0.0005 & Update step size for the network \\
    Learning Rate Decay & 0.99 & Gradually reduces the step size \\
    Discount Factor $\gamma$ & 0.95 & Importance of future rewards \\
    Epsilon (start) & 1.0 & Initial randomness in action choice \\
    Epsilon Min & 0.0001 & Minimum exploration level \\
    Epsilon Decay & 0.955 & How fast randomness decreases \\
    Batch Size & 128 & Samples per training update \\
    Replay Buffer Size & 50,000 & Stored experiences for learning \\
    Target Update Freq & 500 & How often target network updates \\
    Hidden Layers & [384, 320] (ReLU) & Sizes of the network layers \\
    \hline
  \end{tabular}
\end{table}

\section{Training Process}
Training proceeds in federated rounds. For each round, all junction agents interact with the shared SUMO environment for 1000 decisions, collect trajectories, and update their local policies. For CF-MADRL, PPO updates are applied locally and then the federated server clusters agents using K-Means (K=2). Cluster-wise FedAvg is computed and blended with each agent's local weights using $\alpha=0.5$, so each junction keeps some individuality while still benefiting from shared learning. Early stopping is enabled with patience of 10 epochs and a minimum average reward improvement of 0.5. Checkpoints are saved every 10 rounds. Q-Table and DQN baselines use the same environment and step counts, but apply their own update rules: Q-learning updates on discretized states and DQN updates from replay buffer batches with periodic target network syncs.

\section{Evaluation Process}
Evaluation runs all agents (CF-MADRL, Q-Table, and DQN) under the same simulation settings to produce consistent logs. The fixed-time program uses 42 seconds green and 3 seconds yellow per phase, and the simulation runs for 100,000 steps. For learning-based agents, the latest checkpoints are loaded and policies are executed without exploration. Per-junction logs include rewards, queues, and waiting times. Detailed comparisons and results are reported in the Results chapter.

\section{Vehicle Detection}
Vehicle detection is performed on the Raspberry Pi 5 using the YOLO model. The ESP32 streams live video to the Raspberry Pi, where frames are processed to identify vehicles and estimate queue length and waiting time per lane. These signals are then combined with the reinforcement learning (RL) model to select the next traffic phase and duration. The resulting decision is sent back to the ESP32, which drives the traffic lights and the remaining-time display.

\begin{table}[H]
  \centering
  \caption{YOLO monitoring configuration (Raspberry Pi).}
  \label{tab:yolo_config}
  \begin{tabular}{|l|r|}
    \hline
    \textbf{Parameter} & \textbf{Value} \\
    \hline
    Primary Model & best.pt \\
    Fallback Model & yolo26n.pt \\
    Confidence Threshold & 0.3 \\
    Vehicle Classes & car, motorcycle, bus, truck, train \\
    Stop Speed & 3.0 km/h \\
    Pixel-to-Meter & 0.05 \\
    Max Retries & 5 \\
    \hline
  \end{tabular}
\end{table}

\section{Deployment Hardwares: Traffic Control System}
The traffic control stack handles sensing, inference, and actuation for the intersection. It connects the camera feed, on-device inference, and physical signaling into a single closed loop.

\subsection{Raspberry Pi 5 (Main Server)}
The Raspberry Pi 5 is the central controller and inference node for detection and RL. It receives the video feed from the ESP32, runs YOLO to detect vehicles, executes the RL policy to choose the next phase and duration, and sends the decision back to the ESP32 for actuation.
\begin{figure}[H]
  \centering
  \includegraphics[width=0.65\textwidth]{img/components/raspberrypie5.jpeg}
  \par\medskip
  \includegraphics[width=1.0\textwidth, angle=90]{img/schemetic_diagram/raspie_schemetic.png}
  \caption{Raspberry Pi 5}
  \label{fig:rpi5}
\end{figure}

\subsection{ESP32 (Edge Controller)}
The ESP32 acts as the communication, sensing, and actuation interface. It streams camera data to the Raspberry Pi 5, receives the selected phase and duration, and then drives the traffic light LEDs and the OLED countdown display.


\begin{figure}[H]
  \centering
  \includegraphics[width=0.7\textwidth]{img/components/esp32s.png}

  \par\medskip

  \includegraphics[width=1.0\textwidth]{img/schemetic_diagram/esp32_schematic.png}

  \caption{ESP32}
  \label{fig:esp32}
\end{figure}

\subsection{OLED Display}
The OLED display shows the remaining time for the current signal so the active phase is visible at a glance.
\begin{figure}[H]
  \centering
  \includegraphics[width=0.5\textwidth]{img/components/oled_display.png}
  \caption{OLED display}
  \label{fig:oled}
\end{figure}

\subsection{Buck Converter (LM-2596 DC-DC)}
The LM-2596 buck converter provides a stable 5V power supply for the traffic control electronics by stepping down the input voltage.
\begin{figure}[H]
  \centering
  \includegraphics[width=0.5\textwidth]{img/components/buck_converter.jpeg}
  \caption{LM-2596 buck converter}
  \label{fig:lm2596_a}
\end{figure}

\subsection{3D Printed Traffic Stand}
The 3D printed traffic stand holds the ESP32 camera, LED lights, and OLED display in a fixed and aligned position for consistent sensing and signaling.
\begin{figure}[H]
  \centering
  \includegraphics[width=0.6\textwidth]{img/components/traffic_stand.jpeg}

  \par\medskip
  
  \includegraphics[width=0.6\textwidth]{img/components/stand_3d_model.png}
  \caption{Traffic Stand}
  \label{fig:traffic_stand}
\end{figure}

\section{Deployment Hardwares: Line-Follower Vehicle}

The line-follower vehicle validates lane-level movement and control. It serves as a compact physical testbed for sensing, control logic, and actuation.

\subsection{Arduino Nano (Vehicle Controller)}
The Arduino Nano handles line following and motion control. It reads the IR sensor array to estimate line position, computes the line-following error, and generates PWM signals to control speed and direction. It also communicates with the Bluetooth module to support remote start and stop.
\begin{figure}[H]
  \centering
  \includegraphics[width=0.6\textwidth]{img/components/arduino_nano.jpg}
  \caption{Arduino Nano}
  \label{fig:arduino_nano}
\end{figure}

\subsection{QTR-8RC IR Sensor Array}
The QTR-8RC IR sensor array detects the black line position under the chassis and provides the primary navigation signal for the controller.
\begin{figure}[H]
  \centering
  \includegraphics[width=0.5\textwidth]{img/components/ir_sensor_array.jpeg}
  \caption{QTR-8RC array}
  \label{fig:qtr8rc}
\end{figure}

\subsection{TB6612FNG Motor Driver}
The TB6612FNG motor driver drives the N20 gear motors using PWM for precise speed and direction control.
\begin{figure}[H]
  \centering
  \includegraphics[width=0.6\textwidth]{img/components/motor_driver.jpg}
  \caption{TB6612FNG motor driver}
  \label{fig:tb6612fng}
\end{figure}

\subsection{N20 Gear Motors (x2)}
The N20 gear motors provide forward motion and turning by enabling differential speed between the wheels.
\begin{figure}[H]
  \centering
  \includegraphics[width=0.5\textwidth]{img/components/n20_gear_motor.png}
  \caption{N20 gear motors}
  \label{fig:n20_motor}
\end{figure}

\subsection{Wheels (x2)}
The wheels transfer motor torque to the ground for stable movement along the line.
\begin{figure}[H]
  \centering
  \includegraphics[width=0.5\textwidth]{img/components/wheel.jpeg}
  \caption{Wheels}
  \label{fig:wheels}
\end{figure}

\subsection{HC-05 Bluetooth Module}
The HC-05 Bluetooth module provides remote start/stop control. It receives commands from a mobile device and forwards start/stop signals to the Arduino Nano.
\begin{figure}[H]
  \centering
  \includegraphics[width=0.5\textwidth]{img/components/bluetooth_module.png}
  \caption{HC-05 module}
  \label{fig:hc05}
\end{figure}

\subsection{Final Line Follower Vehicle}
The final line follower vehicle combines motors, sensors, and the control unit into a functional system for end-to-end validation.
\begin{figure}[H]
  \centering
  
  % Assembled vehicle image
  \includegraphics[width=0.6\textwidth]{img/components/assembled_vehicle.jpg}
  
  \par\medskip
  
  % Schematic diagram image
  \includegraphics[width=0.9\textwidth]{img/schemetic_diagram/pcb_design_vehicle_image.png}
  
  \caption{Final line follower vehicle}
  \label{fig:final_vehicle}
\end{figure}

\section{Real Hardware Flow}
\subsection{Hardware Working Diagram}
\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{img/working_diagram_final.png}
  \caption{Hardware working diagram.}
  \label{fig:hardware_working}
\end{figure}

The real-hardware data flow is summarized as follows:
\begin{enumerate}
  \item ESP32 captures the camera stream and transmits it to the Raspberry Pi 5.
  \item Raspberry Pi 5 runs YOLO and the RL model to compute the traffic phase.
  \item Raspberry Pi 5 sends the control command to ESP32.
  \item ESP32 drives the traffic light LEDs and OLED timer display.
  \item The line-follower vehicle operates in the lane to validate detection and control logic.
\end{enumerate}

\subsection{ESP32 API Interface}
The Raspberry Pi runs a FastAPI server. ESP32 polls the API at the end of each phase cycle and uses the returned decision to control the physical traffic lights. Optional yellow is applied when the target phase differs from the last phase. The API returns JSON with fields \texttt{phase}, \texttt{duration}, and \texttt{yellow\_required}.

\begin{table}[H]
  \centering
  \caption{Raspberry Pi API for ESP32 control.}
  \label{tab:api_config}
  \begin{tabular}{|l|l|}
    \hline
    \textbf{Item} & \textbf{Detail} \\
    \hline
    Server & FastAPI (uvicorn) on port 8000 \\
    Endpoint & \texttt{GET /get\_phase} \\
    Response Model & \texttt{\{phase, duration, yellow\_required\}} \\
    Default Response & phase=0, duration=30, yellow\_required=false \\
    Phase Mapping & 0 = East-West green, 2 = North-South green \\
    Duration Range & 10-60 s (step 10) \\
    Health Check & \texttt{GET /health} \\
    \hline
  \end{tabular}
\end{table}

Deployment inference uses local queues and waits (padded to \texttt{max\_lanes}=6) plus the current phase (no shared state). In deployment, the action is decoded into one of the durations $\{10,20,30,40,50,60\}$ seconds.
